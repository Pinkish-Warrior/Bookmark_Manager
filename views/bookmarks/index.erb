<!-- in views/bookmarks/index.erb -->
<!-- do we need the file bookmarks? as set on the walkthrough -->

<ul>
  <% @bookmarks.each do |bookmark| %>
    <li>
      <a href="<%= bookmark.url %>">
        <%= bookmark.title %>
      </a>
    </li>
  <% end %>
</ul>
